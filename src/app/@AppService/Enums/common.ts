export enum Language {
  English = 1,
  Arabic = 2,
}

export enum MessageType {
  Success = 0,
  Error = 1,
  Warning = 2,
}

export enum ResultStatusEnum {
  Success = 0,
  Error = 1,
}

export enum SortDirectionEnum {
  Ascending = 0,
  Descending = 1,
}

export enum PostOfficeType {
  Subsidiary = 100,
  Major = 101,
  ExchangeCenter = 102,
}

export enum BillOfLadingStatus{
  Generating = 2600,
  ReadyForPrint = 2601,
  InProgress = 2602,
}

export enum ViewModeEnum
{
  table =1,
  card = 2
} 
